# Workflow Summary

Render `summary.md` from verified manifest and selected-checkpoint fields only. Include route, optimization mode, execution backend, stage statuses, final code, measured performance when available, and an artifact index. Use `N/A（原因：...）` for absent values. Never execute compilation, accuracy, profiling, or benchmark commands while rendering this template.
